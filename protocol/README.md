# protocol/

Planned location for the versioned, shared Drone ↔ GCS interface
definition (schemas/IDL for the message types drafted in
`../docs/DATA_MODELS.md`), once the transport/protocol decision in
`../docs/DECISIONS.md` (D-1, D-2) is made jointly with the drone-side team.

**Phase 0: empty.** Currently the interface exists only as documentation
in `../docs/COMMUNICATION.md` and `../docs/DATA_MODELS.md`.
